#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int n,q,i;
    int a[n];
    for(i=0;i<n;i++)
        cin>>a[i];
    for(i=1;i<=q;i++)
        {
        int x,y,k;
        cin>>x>>y>>k;
        for(j=y-1;j>)
    }

    return 0;
}
